import styled from '@emotion/styled';

const PlayerWrapper = styled.div`
  overflow: hidden;
  padding-top: 56.25%;
  position: relative;
`;

const StyledFrame = styled.iframe`
  border: 0;
  height: 100%;
  left: 0;
  position: absolute;
  top: 0;
  width: 100%;
`;

export { PlayerWrapper, StyledFrame }